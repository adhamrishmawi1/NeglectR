#'example of a dataset we could demonstrate a very basic operation of our func
#'days an dates are included


data2 <- data.frame(
  days = c('Monday', 'Wednesday', 'Friday'),
  dates = c('03/14/23', '04/15/22', '01/01/2000')
)
